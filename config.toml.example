[api]
# ── Option A: Anthropic native API ────────────────────────────────────────────
# provider = "anthropic"
# anthropic_api_key = "sk-ant-..."
# model = "claude-opus-4"

# ── Option B: claude-max-api-proxy (uses your Claude Max subscription) ────────
# Run first: npx claude-max-api-proxy
provider = "openai_compatible"
base_url = "http://localhost:3456/v1"
api_key = "not-needed"
model = "claude-opus-4"

[monitoring]
# Windows Event Log channels to tail
event_log_channels = ["System", "Application", "Security"]

# Directories whose new/modified *.log, *.txt etc. files are parsed for errors
log_directories = [
    "C:\\Windows\\Logs",
    "C:\\ProgramData",
]

# How often to poll WMI for system state (CPU, memory, disk, services)
wmi_poll_interval_secs = 300

# How often to check the event log for new entries
event_log_poll_interval_secs = 30

# How often to run a full decision cycle (collect signals → analyse → act)
decision_interval_secs = 120

[persistence]
audit_db = "./sentry.db"

[logging]
# trace | debug | info | warn | error
level = "info"
